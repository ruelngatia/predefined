// the method evaluates JavaScript code represented as a string.
console.log(eval('789 + 1 '));

// checks if number is a finite number

console.log('is 22/7 finite? :'.concat(isFinite(22/7)))

// parses float to string

console.log(parseFloat('22/7') * 7);

// checks if values is a number

const s = 'Node'

console.log('id value s an number'.concat(isNaN(s)))